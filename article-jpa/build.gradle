dependencies {
  compile project(':article-domain')
  compile project(':account-jpa')
  compile project(':support-spring-jpa')

  testCompile project(path: ':account-domain', configuration: 'testArchives')
  testCompile project(':configuration-jpa')
  testCompile 'com.h2database:h2'
}