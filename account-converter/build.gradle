dependencies {
  implementation project(':account-domain')
  implementation project(':account-dto')

  testCompile project(path: ':account-domain', configuration: 'testArchives')
  testCompile project(path: ':account-dto', configuration: 'testArchives')
}