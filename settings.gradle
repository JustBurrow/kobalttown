rootProject.name = 'kobalttown'

def groups = [
    configuration: ["data-jpa", "web", "bean", "security"],
    page         : ["root", "account"],
    global       : ["security"],
    account      : ["domain", "data", "service", "dto", "converter", "borderline", "web", "dependencies"],
    runner       : ["standalone"]
]

groups.each { feature, layers ->
  layers.each { layer ->
    def module = "$feature-$layer"

    include ":$module"
    findProject(":$module").projectDir = new File("$feature/$module")
  }
}
