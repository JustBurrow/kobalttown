rootProject.name = 'kobalttown'

def groups = [
    account: ["domain"]
]

groups.each { group, layers ->
  layers.each { layer ->
    def module = "$group-$layer"

    include ":$module"
    findProject(":$module").projectDir = new File("$group/$module")
  }
}
